# Copy to .env (or export) - every value is optional for the demo.
LLM_PROVIDER=mock               # mock | openai
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4.1-mini

CELERY_TASK_ALWAYS_EAGER=true   # false = real async; needs Redis + a worker
# CELERY_BROKER_URL=redis://localhost:6379/0

# DJANGO_SECRET_KEY=change-me
# DJANGO_DEBUG=true
